const AccessLevels = require('../../models/accessLevel');
const { message } = require('./generalValidation');

const isAccessLevelBodyValid = (paramsObj) => {

};

module.exports = {
    isAccessLevelBodyValid,
}
